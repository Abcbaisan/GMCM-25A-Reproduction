# 2025 年研究生数学建模 A 题：论文与代码总对照

> **使用方式**：从「论文/赛题研究对象」定位到「可点击源码、函数、输入输出和验证证据」，覆盖问题一、二、三，重点展开问题三。本文对照的是当前仓库的**独立复现与扩展实现**，不代表作者原始程序；也不把“求解器自行报告的数字”当作独立验证。
>
> **来源和核对级别（重要）**：本次直接核对了 `q1_reproduction/q1.py`、`q1_reproduction/学习说明.md`、`q2_reproduction/q2.py`、`q2_reproduction/论文代码对照.md`、`q3_reproduction/q3.py`、`q3_reproduction/README.md`、`q3_reproduction/test_q3.py` 与已提交的 `results/report.json`。仓库的 [优秀论文 PDF](A题-1-通用神经网络处理器下的核内调度问题.pdf) 是二进制文件，**本次未能直接抽取正文并逐页核校**；问题一的页码/算法号、问题二的论文定位均**沿用仓库既有对照记录，属于待原文复核的二手定位**。问题三只做目标/方法主题对照，**不编造论文第三问的页码、公式编号、算法编号或作者实现细节**。赛题见 [原始题面](通用神经网络处理器下的核内调度问题.docx)。

## 0. 三问一张图：输入、输出、依赖

```text
官方 ZIP 中的 Nodes、Edges
       │
       ▼
问题一：q1_reproduction/q1.py
  Graph.initial / compact / anneal / peak / validate
  输出 *_schedule.txt：原图节点的合法拓扑序
       │
       ▼
问题二：q2_reproduction/q2.py
  SpillAllocator.run → MemoryPool + choose_victim + spill_out/restore
  输出三件套：*_schedule.txt、*_memory.txt、*_spill.txt
       │
       ├── q2_reproduction/verify_q2.py：对三件套独立重放、重算额外搬运量
       ▼
问题三：q3_reproduction/q3.py
  PipelineModel 构造依赖与估时 → solve_one 生成/筛选候选 → improve_swaps
  每个候选先验合法性，最终再次验证并输出三件套和 results/report.json
```

- **问题一优化量**：逻辑缓存峰值 `M = max_k Σ_{i≤k} w(s_i)`；`ALLOC` 增加 Size，`FREE` 减少 Size。须明确 `--objective l1ub` 与默认 `all` 的统计口径不同。
- **问题二优化量**：`D = Σ_k Size(b_k) × (1 if b_k 属于原图 COPY_IN else 2)`；一次 OUT/IN **成对**计费，同一缓冲区多次换出重复计费。
- **问题三优化量**：在选定的依赖、流水与 SPILL 周期估算模型下缩短总完成时刻 `T`；本仓库自设 `D_candidate ≤ D_baseline × (1 + tolerance_pct/100)`。默认 **5% 是本地实验参数，不是题目指定常数**。
- **不要混淆**：`M` 是逻辑容量峰值，`D` 是额外数据搬运量，`T` 是估算时钟周期。问题二的 `events` 周期字段本身不意味着已解问题三。

## 1. 问题一：模型/论文记录 → 代码逐项映射

> 论文定位据 [问题一学习说明](q1_reproduction/学习说明.md)，**页码尚待对原 PDF 复核**。程序以 [q1.py](q1_reproduction/q1.py) 的实际行为为准。

| 论文/建模部分（定位来自既有记录） | 实际代码入口 | 精确对应与边界 |
| --- | --- | --- |
| 计算图及缓存变化量；既有记录指第 9、12 页 | `Graph.__init__` 的 `nodes/edges/succ/pred/weight/objective_weight` | 从 Nodes/Edges 建 DAG，申请为 `+Size`、释放为 `-Size`，区分原始生命周期权重与优化权重；`l1ub` 只把 L1/UB 计入目标。 |
| 峰值驻留目标 P1；既有记录指第 12 页 | `Graph.peak(order)` | 沿顺序对 `objective_weight` 前缀求和取最大值；**不是每种类型峰值先各自最大再相加**。 |
| 初始拓扑序、贪心；既有记录指算法 2 | `Graph.initial('greedy')` | Kahn 就绪集合，FREE 优先，按增量和 ID 选点；`paper_score` 为论文口径开关。 |
| 深度优先初始序；既有记录指算法 3 | `Graph.initial('depth')` | 使用入堆先后形成近似深度优先的合法序列；`dfs_rank` **是本地反向 DFS 扩展**，不能当成论文作者原代码。 |
| 关键路径初始序；既有记录指算法 4 | `Graph.height`、`initial('critical')` | `height` 是最长**边数**，不是问题三按 `Cycles` 计算的剩余工作量 `rank`；当前排序遵循既有附录记录。 |
| 模拟退火；既有记录指算法 1 | `Graph.anneal()` | 合法重插入、Metropolis 接受和降温；迭代次数、随机种子、温度参数属于当前实现设定。 |
| 生命周期压缩 | `Graph.compact()` | 延后 ALLOC、提前 FREE，保留合法性；**本地补充**，非已核实的论文独立算法。 |
| 完整性和输出 | `Graph.validate()`、`q1_reproduction/verify_results.py`、`main()` | 检查所有节点/依赖/生命周期及可选 L0 单缓冲区约束，输出原节点拓扑序；验证器是本地增强。 |

**交接给问题二的接口**：`q1_reproduction/reproduction_results/<case>_schedule.txt` 中每行一个原节点 ID，必须为完整拓扑序；问题二读取后加入 SPILL。问题三默认并不直接用这里的文件作为基线，而是使用问题二已生成、已验证的三件套。

## 2. 问题二：论文思路/模型 → 代码逐项映射

> 已有 [问题二专门对照](q2_reproduction/论文代码对照.md) 包含更多细节与历史数值；其中记录的“算法 5—7、正文/附录排序不一致”等**尚未经本次直接逐页核校**。本节只记录从源代码能确认的实现关系。

| 问题二部分 | 函数/对象 | 具体行为、输出与性质 |
| --- | --- | --- |
| 固定 Type、有限连续地址 | `CAPACITIES`、`SpillAllocator.__init__` | 五个内存池 L1/UB/L0A/L0B/L0C；验证输入 order、生命周期和单操作同时需求；当前代码不修改 Type。 |
| 空闲连续区间管理 | `MemoryPool.allocate/release` | 半开区间 `[offset, offset+size)`；Best-Fit/First-Fit 分配，释放时合并相邻空洞；**分配器工程实现细节**。 |
| SPILL 候选选择 | `next_use`、`choose_victim` | `paper-small/paper-large` 用于对照既有文献记录中的两种排序，`farthest/cost-distance` 是本地扩展；不能断言其中某个就是作者唯一源码。 |
| OUT/IN 配对、延迟换入 | `spill_out(b)`、`restore(b,protected)` | 第 k 对编号为 `N+2k`/`N+2k+1`；OUT 释放物理空间但不结束逻辑生命周期；IN 重新分配地址并记录 `new_offset`。 |
| 分配失败、受保护工作集 | `acquire`、`can_place_needed`、`repack` | 分配失败可逐步换出；碎片导致受保护集合无法同时放置时，真实 OUT/IN 重新排布且**照常计费**；`repack` 属本地合法性补充。 |
| 综合执行 | `SpillAllocator.run()` | 遍历原 order，处理 ALLOC/普通操作/FREE，插入 SPILL 并返回 schedule/memory/spills/events/成本/统计。 |
| 额外搬运目标 | `spill_out` 中 `cost`/`self.extra` | `COPY_IN` 缓冲区计 `Size`，其他计 `2×Size`；这是模型计费，不是实际 DDR 张量移动代码。 |
| 独立合法性检验 | `verify_q2.verify_case()` | 重新按地址单元重放正式三文件，检查顺序、地址不重叠、使用时驻留、OUT/IN 配对、重算搬运量。**不是论文原算法**。 |
| 结果文件 | `save_result`、`read_result_files` | 每个案例输出 `<case>_schedule.txt`、`<case>_memory.txt`、`<case>_spill.txt`；`events.json` 仅调试，非官方三件套。 |

**交接给问题三的三个具体数据**：① 最终 `schedule`（含 SPILL），② `memory[BufId]`（首次 ALLOC 偏移），③ `spills[k]=(BufId, SPILL_IN 新偏移)`。不同于问题一，仅有原始 `order` **不足以**计算物理地址复用依赖或完整 SPILL 周期。

## 3. 问题三：目标、时间模型、搜索算法与 `q3.py` 逐段对照（重点）

> 问题三目标是结合前两问方案兼顾总时间与搬运；**下面“论文/模型主题”是研究目标层面的对应，不是已经核对到优秀论文原文某页的算法归属。** [当前 q3.py](q3_reproduction/q3.py) 是本地可执行求解尝试，不能自称论文作者源码，也不保证全局最优。

| 论文/问题三要回答的主题 | 代码精确位置 | 数学/执行含义 | 状态/区别 |
| --- | --- | --- | --- |
| 复用前两问输出 | 模块常量 `DEFAULT_ZIP`、`DEFAULT_BASELINE`、`DEFAULT_ALTERNATIVE`；`solve_one()` | 读取原图、问题二三件套及备选结果；基线来自 `q2_reproduction/runs/q2-20260914/optimized/`。 | 仓库工程交接；未读取原问题一 `order` 作为 Q3 的直接默认基线。 |
| 从图建立原始依赖 | `PipelineModel.__init__`: `nodes`、`orig_edges`、`preds/succ`、`edge()` | `Edges` 进入前驱/后继表；新增边必须与当前 schedule 顺序一致，否则抛错。 | 时间模型基础。 |
| 计算节点的执行单元和时长 | `PipelineModel.__init__`: `pipe[v]`、`cycles[v]` | 普通操作读取原节点 `Pipe/Cycles`；ALLOC/FREE 默认 `pipe=None, cycles=0`。 | 是抽象流水估时，不是实际硬件测量。 |
| SPILL 增广依赖 | `PipelineModel.__init__`: `for k,(b,_) in enumerate(spills)` | 加入 `ALLOC→OUT→IN→FREE`、OUT 前使用→OUT、IN→后续使用、同缓冲区连续 SPILL 的前次 IN→后次 OUT。 | 依赖生成针对已选定三件套；先用 `verify_case` 检查其合法性。 |
| SPILL 所属 Pipe 与周期 | 同一循环中的 `pipe[out_id/in_id]`、`cycles[out_id/in_id]` | OUT 属 MTE3，IN 属 MTE2；属于原图 COPY_IN 的 OUT 为 0 周期，其余 OUT 与全部 IN 为 `2×Size+150`。 | 与 q2 的 `events` 记录口径一致；**与问题二按 Size/2×Size 计费的 `D` 不是同一指标**。 |
| 物理地址复用依赖 | `PipelineModel.__init__` 中局部 `owners`、`last_release`、`acquire()`、`release()` | 逐地址重放：释放地址时记录节点，再申请同一地址时建立“此前释放节点→当前 ALLOC/IN”依赖，并统计 `reuse_edges`。 | 由正式 `memory/spills` 推导，不能只看 DAG 的原始 Edges。 |
| 流水独立、同流水 FIFO | `PipelineModel.makespan(schedule)` | 每条 Pipe 保存 `pipe_finish`；节点开始时间为前驱完成和所属 Pipe 空闲时间的较大值；不同 Pipe 可以重叠。 | 同 Pipe 按提交的 schedule 顺序，不考虑未建模微架构开销。 |
| 总执行时间 | `PipelineModel.makespan()` 的 `latest` | `start(v)=max(max_{u∈pred(v)}finish(u),pipe_finish[pipe(v)])`（无 Pipe 时仅看前驱）；`finish=start+cycles`；`T=max_v finish(v)`。 | **估算**总周期，未核对优秀论文所有数值，也非设备实测。 |
| 利用关键路径指导重排序 | `critical_order(data, free_first=False)` | Kahn 求原图拓扑序，反向递推 `rank[v]=duration[v]+max(rank[succ])`，在 ready 中按 `-rank` 优先；可切换 FREE 优先。 | **本地启发式**，仅基于原始 DAG 和原节点 Cycles，未把固定 SPILL/地址复用依赖纳入该 rank。区别于 Q1 的最长**边数** `height`。 |
| 重排后重新解决内存分配 | `solve_one()` → `SpillAllocator(data, order, policy, placement).run()` | 对原拓扑序及关键路径候选试 `farthest`/`cost-distance`，原顺序还试 Best-Fit/First-Fit；生成完整三件套。 | 必须重新计算内存与 SPILL，不能只改原 order 而沿用原地址清单。 |
| 搬运成本上限 | `solve_one()`: `limit`、`consider()` | `D_limit=D_baseline×(1+tolerance/100)`，超过直接不接受；默认容忍度为 5%。 | **本地人为实验约束**；不可写成题目或优秀论文给定 5%。 |
| 受限候选选择 | `solve_one()` 内 `consider()` | 先 `verify_case`，再估时；按 `(cycles, movement, SPILL 对数)` 字典序更新最佳方案；不改善则保留问题二基线。 | 局部候选比较，没有全局最优证明。 |
| 邻域微调 | `PipelineModel.adjacent_swap_legal()`、`improve_swaps()` | 当前代码可以尝试交换相邻普通操作（含跨 Pipe），拒绝直接依赖边；不移动 ALLOC/FREE/SPILL；候选用 `makespan` 评估，获益才接受，最后再调用验证器。 | **本地新增跨 Pipe 相邻交换**；不是论文算法。旧 README 曾描述“只交换同 Pipe”，与**当前代码**不同，以此处及源码为准。 |
| 落盘前约束与正式输出 | `solve_one()` 的 `final_check`、`save_three()` | 最终再次验证；要求 `D≤limit`、`T≤baseline_T`；写 Q3 的 schedule/memory/spill 三文件。 | 发生异常的算例会记录失败，不可把部分结果说成全量通过。 |
| 批处理与复现报告 | `main()` | 解析命令行，逐个 JSON 调 `solve_one()`，写 `results/report.json`；存在失败则退出码 1。 | `report.json` 的数据来自实际运行，文档不能预填新的实验结果。 |
| 最小回归与独立复核 | `test_q3.py`、`verify_q2.py` | 小图测试流水 121→111、地址复用边、关键路径拓扑性质；正式结果需再通过 `verify_q2.py`。 | 测试文件包含**预期断言**，本次撰写未重新执行测试或全部六个案例。 |

### 3.1 论文语言如何准确转为数学式与代码

设操作节点为 `v`，前驱集合 `Pred(v)` 包含原图边、SPILL 衔接和地址复用边。令 `P(v)` 为执行 Pipe；该 Pipe 上、在当前线性序列中已排在 `v` 前的最后一个操作的完成时刻记为 `F_pipe`。当前实现按序计算：

```text
DependencyReady(v) = max(finish(u) for u in Pred(v))，空集合取 0
start(v) = max(DependencyReady(v), F_pipe[P(v)])    # ALLOC/FREE 不占 Pipe
finish(v) = start(v) + cycles(v)
T = max_v finish(v)
```

与 `q3.py` 的 `makespan()` 一一对应：`dep_end → start → end → finish[v]/pipe_finish[pipe] → latest`。这是一种**给定顺序下的最早开始模拟**，不是 ILP 求全局最优，也没有真正发出 GPU/NPU 指令。不同 Pipe 能否重叠由 `preds` 和各自 `pipe_finish` 共同决定。

### 3.2 重点：三个容易误写成“论文复现”的地方

1. **`critical_order` 不等于作者问题三的已核验算法。** 它只以原图 `Cycles` 定义反向最长剩余工时，切换 FREE 优先，然后调用问题二重新分配；没有证据支持“优秀论文原样如此实现”。
2. **`improve_swaps` 是本地扩展。** 当前源码允许相邻普通节点跨 Pipe 交换；旧版 README 的“只交换同 Pipe”已过时。只优化少量相邻邻居不保证有提升，且 `--max-swaps` 会抽样限制候选。
3. **`5%` 与方案筛选顺序是实验设定。** `T` 主排序，`D` 与 SPILL 对数用于打破平局；并非经原论文逐条核对过的目标函数或权重。基线不劣化由代码保证，最优性不由此保证。

### 3.3 贯穿一个小图：为什么要估时而不仅看搬运量

`q3_reproduction/test_q3.py::toy_case()` 定义 A、B 两组独立 COPY_IN（同一 MTE2）和 MATMUL（CUBE）。原序列 `[0,1,2,3,4,5,6,7]`，模型中的预期值为 121 cycles。把相邻独立的节点 2、3 交换为 `[0,1,3,2,4,5,6,7]`，测试断言预期 111 cycles；`memory={10:0,20:100}`、`spills=[]` 不变。这是**测试中声明的预期小算例**，不等于本次跑出的六个官方结果。

## 4. 结果与“已验证”的边界

- 仓库存在 [问题三已提交的 report.json](q3_reproduction/results/report.json) 和各案例正式三文件。查看时必须同时检查对应 commit、`selected_candidate`、`validated`、`baseline_cycles/optimized_cycles`、`baseline_extra_data/optimized_extra_data`，不能只看 `optimized_cycles`。
- 本次读取到的已提交 `report.json` 中，`Conv_Case0` 的基线和选中值都为 **476530 cycles / 46872 搬运量**，选中 `q2-optimized-baseline`，`swap_search.evaluated_swaps=0`；这只能反映**该报告产生时的实验版本**，不能冒充当前跨 Pipe 交换代码的最新运行结果。
- 现有报告记录六个案例的 `validated` 字段，**本次未在本地重新执行六案例、未重新核对论文中的问题三数值**。文档不声称“论文表格已数值复现”。
- [GitHub Actions 工作流](.github/workflows/q3-reproduction.yml) 配置为测试 → 解六案例 → `verify_q2.py` 复核 → 全部成功后提交结果；工作流配置存在**不等于**最新提交已经完成运行，应核查 Actions 记录与结果提交 SHA。

## 5. 沿着论文阅读时的代码导航

| 想弄懂什么 | 建议阅读顺序 |
| --- | --- |
| 为什么问题二输出是问题三输入 | `q2_reproduction/q2.py::run/save_result` → `q2_reproduction/verify_q2.py::read_result_files` → `q3_reproduction/q3.py::solve_one` |
| SPILL 成本和时间为什么不同 | `q2.py::spill_out` 的 `cost/out_cycles/in_cycles` → `verify_q2.py::verify_case` 末尾成本重算 → `q3.py::PipelineModel.__init__` 设置 `cycles` |
| 物理地址复用为什么影响执行时间 | `verify_q2.py` 的 `owners/last_release` 检查 → `q3.py::PipelineModel.__init__` 的同名数组及 `edge(previous,v)` |
| 多流水并行如何估时 | `PipelineModel.__init__` 建立 `preds/pipe/cycles` → `PipelineModel.makespan` |
| 重排原图如何影响额外搬运 | `critical_order` → `SpillAllocator.run` → `verify_case` → `solve_one.consider` |
| 优化为何可能没有实际提升 | `solve_one` 的 `limit` 和候选筛选 → `improve_swaps` 的预算、邻域 → `results/report.json` 的 `trials` |

## 6. 如何复查，及后续必须补齐的证据

在仓库根目录执行（示例使用 Bash；Windows CMD 可改为单行）：

```bash
python -m unittest discover -s q3_reproduction -p 'test_q3.py' -v
python q3_reproduction/q3.py --case all --tolerance-pct 5 --max-swaps 64 --swap-passes 2
python q2_reproduction/verify_q2.py --zip '通用神经网络处理器下的核内调度问题附件.zip' --results q3_reproduction/results
```

**待完成、不得用推测代替**：① 直接查看优秀论文 PDF，对本文件问题一/二沿用的页码与算法名逐项核校；② 为问题三补充真正的 PDF 页码、公式/表格编号、作者原文描述；③ 复核题面附录 C 的执行模型与本地 `PipelineModel` 完全一致与否；④ 在运行最新代码后更新六案例 `report.json`，记录提交 SHA、周期、搬运量和验证结果；⑤ 如需论文严格复现，单列“作者算法”与“本地候选搜索”实验组，不能混用结果。

---

**核对原则**：赛题/论文目标、论文作者确实公开的算法、仓库可执行实现、本地扩展、测试预期和历史实验数值，必须分别陈述；证据不足时标注“待原文核校”，不要补造论文内容。
